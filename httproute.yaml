apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fitness-route
spec:
  parentRefs:
  - name: iyas-gateway

  rules:
  - backendRefs:
    - name: fitness-tracker-service
      port: 80